<div class="container">
  <h1 class="form-title">アカウント情報編集</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3 form-wrapper">
      <%= form_with model: @user, local:true do |f| %>
      <%= render "layouts/error_message", model: f.object %>
      <div class="form-group">
          <%= attachment_image_tag @user, :profile_image, format: "jpg", size: "100x100", fallback: "no_image-.jpg" %>
          <%= f.label :profile_image, "プロフィール画像"%>
          <%= f.attachment_field :profile_image %>
        </div>
        <div class="form-group">
          <%= f.label :name, "ニックネーム", autofocus: ""  %>
          <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :introduction, "紹介文" %>
          <%= f.text_area :introduction, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :email, "メールアドレス" %>
          <%= f.email_field :email, class: "form-control" %>
        </div>
        <!---->
        <!--<#% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>-->
        <!--  <div>Currently waiting confirmation for: <#%= resource.unconfirmed_email %></div>-->
        <!--<#% end %>-->
        <!--<div class="form-group">-->
        <!--  <#%= f.label :password, "パスワード変更(変更の場合)" %>-->
        <!--  <#%= f.password_field :password, class: "form-control", placeholder: "半角英数6文字以上" %>-->
        <!--</div>-->
        <!--<div class="form-group">-->
        <!--  <#%= f.label :password_confirmation, "パスワード変更(確認)" %>-->
        <!--  <#%= f.password_field :password_confirmation, class: "form-control", placeholder: "再度ご入力ください" %>-->
        <!--</div>-->
        <!--<div class="form-group">-->
        <!--  <#%= f.label :current_password, "変更にはパスワードが必要です" %>-->
        <!--  <#%= f.password_field :current_password, class: "form-control", placeholder: "必須項目です" %>-->
        <!--</div>-->
        <%= f.submit "変更する", class: "btn btn-primary form-submit-btn" %>
      <% end %>
      <%= link_to "退会する", user_withdraw_path(current_user), class: "btn btn-danger",
                                                                data: {confirm: "本当に退会しますか？今までの記録は削除されます"}  %>
    </div>
  </div>
</div>


