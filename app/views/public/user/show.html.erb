<h1>mypage</h1>
<%= flash[:notice]  %>
<%= link_to "マイページ編集", edit_user_path(@user.id) %>


<% if user_signed_in? %>
  <% if @user != current_user %>
    <% if current_user.following?(@user) %>
    <!--引数にフォローするuser_idを渡す-->
      <%= link_to "フォローを外す", user_relationships_path(user_id: @user.id), method: :delete %>
    <% else %>
      <%= link_to "フォローをする",  user_relationships_path(user_id: @user.id), method: :post %>
    <% end %>
  <% end %>
<% end %>

フォロー<%= link_to @user.followings.count, user_follow_path(@user.id)  %>

フォロワー<%= link_to @user.followers.count, user_follow_path(@user.id)  %>